{
  "name": "skill-progression-coach",
  "version": "1.0.0",
  "private": true,
  "description": "Skill Progression Coach — standalone installable PWA (green). Reads Pull-Up Coach history read-only.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
